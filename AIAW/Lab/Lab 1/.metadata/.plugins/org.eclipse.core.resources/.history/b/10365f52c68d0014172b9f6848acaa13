package eightPuzzle;

public class Board {
	private byte[][] board_;
	
	public Board(byte[][] board) {
		if (null == board || 8 != board.length) {
			throw new IllegalArgumentException();
		}
		
		board_ = new byte[8][8];
		
		for (short[] b: board) {
			Arrays.cop
		}
	}

}
